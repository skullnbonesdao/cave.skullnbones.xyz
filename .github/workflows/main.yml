name: Build Vue
on: [ push ]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
      - uses: actions/checkout@v2
      - id: Build-Vue
        uses: xRealNeon/VuePagesAction@1.0.0
        with:
          username: 'skullnbonesdao'
          reponame: 'cave.skullnbones.xyz'
          token: ${{ secrets.GITHUB_TOKEN }}
      - run: cp index.html 404.html
